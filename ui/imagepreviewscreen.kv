<ImagePreviewScreen>:
  name: "preview"

  BoxLayout:
    id: preview_box
    orientation: "vertical"
    spacing: dp(25)
    padding: dp(50)

    Image:
      id: image_preview
      fit_mode: "contain" # or scale-down
      source: root.current_image["path"] if root.current_image is not None else ""